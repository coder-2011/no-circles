# File: `next-env.d.ts`

## Purpose
Generated Next.js TypeScript ambient references file.

## Behavior
- Provides base Next.js type references.
- May include route-type reference (`./.next/types/routes.d.ts`) when typed routes are generated.

## Maintenance Notes
- Treat as generated output; avoid manual edits when possible.
- Lint ignores are configured in `eslint.config.mjs` for this file because Next.js emits triple-slash references here.
